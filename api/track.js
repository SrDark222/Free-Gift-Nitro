let ips = new Set();export default async function handler(req,res){let ip=req.headers['x-forwarded-for']||req.connection.remoteAddress;if(ips.has(ip))return res.status(200).end();ips.add(ip);let ua=req.headers['user-agent']||'';let data={ip,ua,date:new Date().toISOString()};await fetch("https://teu-webhook-aqui", {method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({content:`Novo acesso:\n\`\`\`\nIP: ${ip}\nUA: ${ua}\n\`\`\``})});res.status(200).end()}
